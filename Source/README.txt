To compile the source code, first download and install Qt:

http://qt-project.org/downloads

Open osFWUpdater.pro in QtCreator. Then compile and build
the application.

You can also compile the code in command line by running
'qmake' followed by 'make'.
